---
description: 新模块开发时对齐学习足迹/作品影像等标杆：实用性优先、美观一致、可控依赖
globs: src/**/*.vue,src/data/**/*.{js,json},src/router/**/*.js
alwaysApply: false
---

# 新模块开发：实用性与界面标杆

新增或扩展**整段功能模块**（首页大区块、独立视图、复杂交互区）时，在遵守仓库既有规则的前提下，以**可交付的实用体验**为第一目标，视觉与交互与现网**同一套语言**。请先阅读并遵循：

- **`.cursor/rules/ui-visual-style.mdc`**（配色、字体、标题层级、毛玻璃与阴影）
- **`.cursor/rules/ui-screen-adaptation.mdc`**（`--page-*` 变量、断点、安全区、`prefers-reduced-motion`）

以下条款**不重复**上述两份的细节，只规定「新模块应达到的质量」与**可参考的实现**。

## 标杆：学习足迹（`ProjectsSection.vue`）

设计新列表/时间线/档案类模块时，应对齐这类**已验证**模式：

- **双栏信息架构**：一侧**固定心智模型**（前缀行 + 主副标题 + 简短说明），一侧**可滚动主内容**；窄屏改为纵向堆叠并与现有断点（如 900px）一致。
- **快速定位**：长列表提供**目录/索引**（如左侧 rail），支持**点击跳转**与**滚动同步高亮**；键盘可聚焦的控件需有 `aria-label`、`aria-current` 等，与现有 `nav`/`role="button"` 用法一致。
- **单卡片信息完整**：主标题、摘要、**元数据**（如日期与 `datetime`）、标签区、明确**下一步**（如「查看详情」）；点击行为与路由约定清晰。
- **数据与文案分离**：条目来自 `projects.json` 等数据文件，展示文案走 **vue-i18n**（`nameKey` / `descriptionKey` / `indexLabels` 等），避免把大段文案硬编码在组件里。
- **性能与滚动**：与顶栏高度相关的 `scroll-margin`、Intersection/滚动节流等**沿用组件内已有注释与写法**，避免重复造轮子导致高亮乱跳。

## 标杆：作品影像（`GallerySection.vue`）

设计新媒体橱窗、筛选、分页类模块时，应对齐这类**已验证**模式：

- **工具条合一**：筛选、搜索、分页、跳转等控制放在**同一视觉簇**，并设合适的 **`aria-label` / `role`**（如 `progressbar`、`toolbar` 区域标签），避免「只有鼠标能用」。
- **主舞台 + 详情**：列表/缩略图与**放大查看**（如 `Teleport` 全屏层）分离；焦点与点击区域明确，关闭方式符合习惯。
- **规模可控**：条目多时**分页或分块渲染**（如 `PAGE_SIZE`）、图片 `loading`/`fetchpriority` 策略与现有一致，避免一次挂载过量 DOM。
- **空状态与边界**：筛选无结果时提供**可操作提示**（如 `emptyFiltered`），不留下空白沉默区。
- **文案与数据键一致**：`gallery.items.{id}` 与组件内 `sourceItems` 的 `id` 对应关系在 **`cn.js` / `en.js` 注释**中已有约定；新增条目时**同步**数据与 i18n，避免运行时缺键。

## 实用性清单（新模块自检）

- **任务闭环**：用户进入模块后能否在 3 步内完成主任务？次要入口是否干扰主路径？
- **可访问性**：键盘能否走完主要流程？语义标签与 `aria-*` 是否与 `ProjectsSection` / `GallerySection` 同级？
- **国际化**：用户可见字符串是否均走 `t()`，且中英文键同步维护？
- **主题**：亮/暗色下对比度与边框是否仍清晰（依赖 CSS 变量，见 visual-style 规则）？
- **首屏与重量**：若模块较重，是否采用与 **`HomeView.vue`** 类似的**视口哨兵 + 异步组件**再挂载，避免拖慢首屏？

## 可控依赖与风险（避免「无法掌控」）

- **不默认引入**新的第三方统计、广告脚本、在线字体 CDN（本仓库已有 `index.html` 字体与统计约定；若确需扩展，应先有**明确接入文档与 DOM 约定**，类似 `docs/technical/不蒜子接入说明.md` 的做法）。
- **不依赖**无 SLA 的外部接口作为**关键渲染路径**；若必须请求网络，应有**降级 UI**（占位、重试或静默失败），且不在路由切换后让旧请求**错误写回**界面（可参考现有请求中止与 URL 校验模式）。
- **不引入**与本站 `main.css` token 完全割裂的新主题系统，以免明暗切换与维护成本失控。

## 与「只做表面」的区别

美观是**结果**：在**信息结构清楚、交互可预测、边界有反馈**的前提下，复用 `--section-heading-*`、`--page-content-*` 与邻近组件的阴影/圆角习惯，即能与学习足迹、作品影像**同一质感**。禁止仅为「好看」增加装饰性动画或遮挡可读性的特效；若新增动效，须处理 `prefers-reduced-motion`（见 screen-adaptation 规则）。
