---
description: 本仓库 Git 提交流程（add . + commit -m，Conventional Commits）
alwaysApply: true
---

# Git 提交流程

## 固定动作（无特殊说明时一律如此）

当用户要求**提交代码**、**commit**、**git 提交**或同类表述时，在仓库根目录按顺序执行（除非用户当次明确改口）：

1. **`git add .`**
2. **`git commit -m "…"`** — 引号内为**符合 Conventional Commits 的提交说明**（用 `-m`，不是 `-u`）

即：先暂存全部未忽略改动，再带一条说明提交。说明格式见下文。

## 提交说明（Conventional Commits）

默认采用 **[Conventional Commits](https://www.conventionalcommits.org/)** 形式：

- **格式**：`<type>: <subject>`；可选范围：`<type>(<scope>): <subject>`
- **type**（小写英文）：`feat` 新功能、`fix` 修复、`docs` 文档、`style` 格式/样式（不改逻辑）、`refactor` 重构、`perf` 性能、`test` 测试、`chore` 构建/工具/杂项、`ci` CI 等。
- **subject**：**默认使用中文**，简短陈述做了什么；避免整段只有英文而无中文语义（除非用户当次要求全英文说明）。

**示例**：

- `fix: 修复不蒜子重复请求导致的浏览量与底栏统计异常`
- `feat: 展会报名导出 Excel`
- `chore: 升级 Vite 到 6.x`
- `docs: 补充本地开发环境说明`

若用户当次明确要求**全英文 subject** 或**不要 type 前缀**，再按其当次要求书写。

**不要**为「写说明」而刻意跑大量诊断命令，除非用户明确要求多段正文或详细 body（多行可用多次 `-m` 或编辑器提交）。

## 辅助用的 Git 命令

为判断改动、确认状态而执行的 `git status`、`git diff`（等只读/查看类命令）**属正常操作**；在 Cursor 中**可直接运行**，**不需要**用户为每次 `run` 再手动点确认。仅在可能改写远端或高危操作前，仍应遵循环境与安全相关约束（若有）。

## 其他

- 已受 `.gitignore` 忽略的路径（如 `target/`）不会被 `git add .` 纳入；若出现未忽略的大文件或密钥，先提醒用户再决定是否提交。
- 仅当用户**明确要求推送**时再执行 `git push`。
