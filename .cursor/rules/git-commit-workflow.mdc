---
description: 本仓库 Git 提交流程（用户指定：add . + commit -m）
alwaysApply: true
---

# Git 提交流程

## 固定动作（无特殊说明时一律如此）

当用户要求**提交代码**、**commit**、**git 提交**或同类表述时，在仓库根目录按顺序执行（除非用户当次明确改口）：

1. **`git add .`**
2. **`git commit -m "…"`** — 引号内为**对应的中文提交说明**（用 `-m`，不是 `-u`）

即：先暂存全部未忽略改动，再带一条说明提交。说明内容见下文。

## 提交说明

根据当前改动写**一句**清晰说明，**默认使用中文**（主体为中文，避免整句全英文）。

- 语气：简短陈述做了什么，如「修复登录态过期未跳转」「调整导航栏布局」。
- 示例：`修复 Quartz 任务日志查询条件`、`feat: 展会报名导出 Excel`（前缀可用英文 type，说明仍用中文）。
- 若用户当次明确要求**英文**或 **Conventional Commits**（全英文 type/subject），再按其要求书写。
- **不要**为「写说明」而刻意跑大量诊断命令，除非用户明确要求多段正文或详细 body（多行可用多次 `-m` 或编辑器提交）。

## 辅助用的 Git 命令

为判断改动、确认状态而执行的 `git status`、`git diff`（等只读/查看类命令）**属正常操作**；在 Cursor 中**可直接运行**，**不需要**用户为每次 `run` 再手动点确认。仅在可能改写远端或高危操作前，仍应遵循环境与安全相关约束（若有）。

## 其他

- 已受 `.gitignore` 忽略的路径（如 `target/`）不会被 `git add .` 纳入；若出现未忽略的大文件或密钥，先提醒用户再决定是否提交。
- 仅当用户**明确要求推送**时再执行 `git push`。
